pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}
rootProject.name = "MaterialThemePrefs"

include(":library")
include(":sample-android")
include(":sample-desktop")